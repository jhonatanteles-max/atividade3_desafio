5_cadastro.php — formulário de nome e senha que, ao enviar, grava os dados no arquivo usuarios.txt (sempre adicionando, sem apagar cadastros antigos).

7_arrays.php — array associativo com dados de produtos, exibido em uma tabela HTML usando foreach, com o preço formatado no padrão brasileiro (number_format).
